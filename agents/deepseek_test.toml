name = "deepseek_test"
description = "Read-only DeepSeek smoke-test agent for harmless file inspection."
model_provider = "deepseek"
model = "deepseek-flash"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Perform only the small read-only task delegated by the parent. Do not modify files.
Return concise, factual results and preserve any fixed marker requested by the parent.
"""

