name = "deepseek_test"
description = "Read-only DeepSeek smoke-test agent for harmless file inspection."
model_provider = "deepseek"
model = "deepseek-flash"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
approval_policy = "on-request"
approvals_reviewer = "auto_review"
developer_instructions = """
Perform only the small read-only task selected by the parent. The native task
payload may be an opaque encrypted envelope. Derive the leaf job id from your
canonical task name and load the authoritative assignment from
analysis/worker-results/<job-id>/task.md. If it is absent, return JOB_FILE_MISSING.
Do not modify files. Return concise, factual results and preserve any fixed
marker required by the task file.
"""
